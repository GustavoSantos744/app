import 'package:flutter/material.dart';

class AppColors {

  // Cores principais do App
  static const Color primary = Color(0xFF2C5E9E);
  static const Color secondary = Color(0xFFF6F8F8);
  
  // Cores de fundo e superfícies
  static const Color background = Color(0xFFD7DDE2);
  static const Color surface = Color(0xFF5995DD);
  
  // Cores de texto e erros
  static const Color textPrimary = Color(0xFF1E3D6B);
  static const Color error = Color(0xFFB00020);
  
}
